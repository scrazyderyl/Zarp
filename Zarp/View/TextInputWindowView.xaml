<Window x:Class="Zarp.View.TextInputView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Zarp.View"
        xmlns:viewModel="clr-namespace:Zarp.ViewModel"
        mc:Ignorable="d"
        Title="Input Window" SizeToContent="WidthAndHeight">
    <Window.DataContext>
        <viewModel:TextInputViewModel/>
    </Window.DataContext>
    <Grid Margin="20,20,20,20">
        <StackPanel Width="200">
            <TextBlock x:Name="Instructions" FontSize="14"/>
            <TextBox x:Name="TextField" Margin="0,8,0,0" FontSize="16"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                <Button Content="Confirm"/>
                <Button Content="Cancel" Margin="20,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
