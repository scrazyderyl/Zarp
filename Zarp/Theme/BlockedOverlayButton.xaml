<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button" x:Key="BlockedOverlayButton">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <Button Content="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Content}" FontWeight="Bold" Cursor="Hand" FontSize="20" Padding="8,2,8,2" Margin="10,0,10,0" BorderThickness="0,0,0,0" BorderBrush="{x:Null}" Background="{x:Null}"></Button>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>
</ResourceDictionary>